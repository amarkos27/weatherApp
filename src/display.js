const Display = (() => {
  const header = document.querySelector('.main header');
  const hourlyConditions = document.querySelector('.hourly-conditions');
  const loadingIcon = document.getElementById('.loading');
})();
